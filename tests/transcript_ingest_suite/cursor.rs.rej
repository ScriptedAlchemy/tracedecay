diff a/tests/transcript_ingest_suite/cursor.rs b/tests/transcript_ingest_suite/cursor.rs	(rejected hunks)
@@ -351,6 +351,10 @@ async fn cursor_pre_compact_uses_cursor_agent_summary_for_lcm() {
     let outcome = cursor_pre_compact_via_daemon(&event.to_string()).await;
     assert_eq!(outcome.status, "ok", "{}", outcome.reason);
     assert_eq!(outcome.summary_nodes_created, 1);
+    assert_eq!(
+        outcome.relation_projection_status,
+        tracedecay::sessions::lcm::LcmRelationProjectionStatus::Pending
+    );
 
     // The daemon is the sole writer authority for its session store. Stop it
     // before mounting the persisted database for post-run assertions.
