diff a/src/mcp/tools/handlers/mod.rs b/src/mcp/tools/handlers/mod.rs	(rejected hunks)
@@ -89,7 +89,8 @@ mod tool_definition_tests;
 
 pub use lcm_tool_entry::handle_user_lcm_tool;
 pub(crate) use lcm_tool_entry::{
-    handle_user_lcm_tool_with_db, handle_user_lcm_tool_with_retained_authority,
+    handle_user_lcm_tool_with_db, handle_user_lcm_tool_with_db_and_controls,
+    handle_user_lcm_tool_with_retained_authority,
 };
 pub use session_authorities::SessionAuthorities;
 use std::path::Path;
@@ -403,6 +404,10 @@ pub fn handle_tool_call_with_registry_and_implicit_project<'a>(
             cg,
             active_project_session_db,
             options.session_authorities.project_retrieval,
+        )
+        .with_effect_controls(
+            options.application_deadline.as_ref(),
+            options.application_cancellation.as_ref(),
         );
         // Classify before moving `args` so large payloads are not cloned into every
         // group probe. Application-surface tools still run before catalog checks;
