diff a/src/mcp/tools/handlers/session/lcm_storage.rs b/src/mcp/tools/handlers/session/lcm_storage.rs	(rejected hunks)
@@ -7,6 +7,8 @@ pub(in super::super) struct LcmHandlerContext<'a> {
     retained_session_db: Option<&'a Arc<RegisteredGlobalDb>>,
     pub(super) retrieval_service: Option<&'a dyn SessionRetrievalServicePort>,
     pub(super) retrieval_store_scope: SessionRetrievalStoreScope,
+    effect_deadline: Option<&'a tracedecay_application::Deadline>,
+    effect_cancellation: Option<&'a tracedecay_application::CancellationSignal>,
 }
 
 impl<'a> LcmHandlerContext<'a> {
@@ -20,6 +22,8 @@ impl<'a> LcmHandlerContext<'a> {
             retained_session_db,
             retrieval_service,
             retrieval_store_scope: SessionRetrievalStoreScope::Project,
+            effect_deadline: None,
+            effect_cancellation: None,
         }
     }
 
@@ -33,9 +37,21 @@ impl<'a> LcmHandlerContext<'a> {
             retained_session_db,
             retrieval_service,
             retrieval_store_scope: SessionRetrievalStoreScope::Profile,
+            effect_deadline: None,
+            effect_cancellation: None,
         }
     }
 
+    pub(in super::super) fn with_effect_controls(
+        mut self,
+        deadline: Option<&'a tracedecay_application::Deadline>,
+        cancellation: Option<&'a tracedecay_application::CancellationSignal>,
+    ) -> Self {
+        self.effect_deadline = deadline;
+        self.effect_cancellation = cancellation;
+        self
+    }
+
     #[cfg(test)]
     pub(super) fn project_for_test(
         project_root: &'a Path,
@@ -47,6 +63,8 @@ impl<'a> LcmHandlerContext<'a> {
             retained_session_db: None,
             retrieval_service,
             retrieval_store_scope: SessionRetrievalStoreScope::Project,
+            effect_deadline: None,
+            effect_cancellation: None,
         }
     }
 }
@@ -64,6 +82,7 @@ fn lcm_unavailable(args: &Value) -> ToolResult {
 
 pub(super) struct LcmStorage {
     pub(super) db: Arc<RegisteredGlobalDb>,
+    pub(super) effects: crate::daemon::lcm_effects::DaemonLcmEffectService,
 }
 
 pub(super) enum LcmStorageResolution {
@@ -91,7 +110,14 @@ pub(super) async fn open_lcm_storage(
     _mode: LcmOpenMode,
 ) -> LcmStorageResolution {
     if let Some(db) = context.retained_session_db {
-        return LcmStorageResolution::Available(Box::new(LcmStorage { db: Arc::clone(db) }));
+        return LcmStorageResolution::Available(Box::new(LcmStorage {
+            db: Arc::clone(db),
+            effects: crate::daemon::lcm_effects::DaemonLcmEffectService::new(
+                Arc::clone(db),
+                context.effect_deadline,
+                context.effect_cancellation,
+            ),
+        }));
     }
     LcmStorageResolution::Unavailable(lcm_unavailable(args))
 }
