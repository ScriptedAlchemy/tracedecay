diff a/src/mcp/tools/handlers/session/lcm_handlers/status/tests.rs b/src/mcp/tools/handlers/session/lcm_handlers/status/tests.rs	(rejected hunks)
@@ -7,15 +7,9 @@ use super::*;
 
 #[tokio::test]
 async fn malformed_doctor_controls_are_rejected_before_storage_open() {
-    for (args, field) in [
-        (
-            json!({"provider": "claude", "mode": false, "format": "json"}),
-            "mode",
-        ),
-        (
-            json!({"provider": "claude", "apply": "yes", "format": "json"}),
-            "apply",
-        ),
+    for args in [
+        json!({"provider": "claude", "mode": false, "format": "json"}),
+        json!({"provider": "claude", "mode": "repair", "format": "json"}),
     ] {
         let error = handle_lcm_doctor(
             LcmHandlerContext::user(Path::new("/missing"), None, None),
@@ -23,7 +17,7 @@ async fn malformed_doctor_controls_are_rejected_before_storage_open() {
         )
         .await
         .unwrap_err();
-        assert!(error.to_string().contains(field), "{error}");
+        assert!(error.to_string().contains("mode"), "{error}");
     }
 }
 
