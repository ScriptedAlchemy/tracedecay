//! Root shim for Tarjan's strongly-connected-components algorithm.
//!
//! The implementation lives in `tracedecay_usecases::graph::scc`
//! (canonical copy; see SEAMS.md). This module keeps every historical
//! `crate::graph::scc::…` path resolving from the root crate.

pub use tracedecay_usecases::graph::scc::*;
