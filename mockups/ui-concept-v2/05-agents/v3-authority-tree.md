# Agents: v3 authority tree

- **Asset:** `v3-authority-tree.png`
- **Lifecycle:** `current`

## Intent

Authority tree without PID or CPU theatre.

## Entry condition

Open `/agents` after the relevant authority responds.

## Visible state

Usage, hierarchy, handoff frontier, tokens, and failures stay distinct.

## Supported interactions

- Depicted: ready usage, stale diagnostics, ready subagent tree/work graph, schema-refused handoff tokens, and failure-context categories.
- It does not execute delegation, handoff, or recovery.

## Truth boundary

This is a `CONCEPT / SYNTHETIC` lookbook plate, not runtime evidence. It establishes no production data, authority availability, counts, health, freshness, persistence, or control. Any unavailable production path remains visibly unavailable.

## Lifecycle history

Pre-Task-1 canonical selection for Agents. Lifecycle is an explicit editorial decision; the version stem records iteration order only.
