# 03 Loom — v6 host weave overview

Lookbook still. CONCEPT / SYNTHETIC. Not a visual-audit golden. Not acceptance.

## Sequence slot

Loom (4): **weave overview (this plate)** → session hover → selected session → zoomed time range.

## Shell

One left rail. 03 Loom selected. Same 01–14 order. No top product rail. Status `LCM PAGE LOADED` is separate from the mark. No SYSTEM NOMINAL. No forbidden extra surfaces.

## Weave

Time increases downward. Hosts across (`host:alpha`, `host:beta`, `host:gamma`). Vertical session strands under each host. Width = messages. Ends: OPEN / RECORDED / UNKNOWN. Playback = loaded LCM page, not an SSE tail. No providers in lane headers. No git text on strands. No drawn causal crossings. Causal evidence appears in the selected-session rail and is not drawn at overview.

## Blocking defects from v5

Cleared: strands are vertical; no live-tail copy; no host-pair causal counts.

## Remaining known defect

None of the v5 blockers remain on this still. Pair-level: rail width and wordmark are not yet identical to Brain v9. OPEN end is labeled “live cap” (end state, not a tail).
