# 03 Loom — v5 host weave overview

Lookbook still. CONCEPT / SYNTHETIC. Not a visual-audit golden. Not acceptance.

## Sequence slot

Loom required sequence (4): **weave overview (this plate)** → session hover → selected session → zoomed time range.

## What this still is

Measured session weave over the **loaded LCM page**. Time down. Hosts across. Strand = session. Thickness = messages. Caps = open vs recorded.

Playback is the loaded page, not an invented SSE tail.

## Shell

- One left rail, expanded (~192px). Same 01–14 order. 03 Loom selected. No top product rail.
- Trace-tail mark is static identity. Page authority (`LCM PAGE LOADED`) is separate. No SYSTEM NOMINAL.
- No Tools, Integrations, Policies, Status, Audit, Labs, Stores, Budget, Orphans, Observer.

## Weave

- Column headers are hosts (`host:alpha` …). Providers belong in session detail on a later still, not lane headers.
- Causal crossings are listed, never drawn on the weave.
- No git hashes, commit subjects, or file names on strands.

## Typed absence (not shown here)

loading · partial · stale · offline · unavailable · denied · unsupported_schema · measured empty (no sessions) · transport failure of the LCM timeline.

## Remaining still defects

- Strands read horizontal across hosts. Time-down requires vertical strands under each host.
- Legend “live tail” fights “loaded LCM page”.
- Causal list as host-pair counts (`alpha → beta 3`) is not a served crossing identity.
