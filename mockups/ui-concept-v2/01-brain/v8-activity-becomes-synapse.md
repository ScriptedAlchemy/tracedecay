# 01 Brain — v8 activity becomes synapse

Lookbook still. CONCEPT / SYNTHETIC. Not a visual-audit golden. Not acceptance.

## Sequence slot

Brain required sequence (5): registry idle → project hover → repository zoom → scoped project → **real activity (this plate)**.

## What this still is

Unscoped registry field at the moment one admitted activity is accepted. A synapse is any real admitted activity touching a project or worktree, not a hook-only icon.

Banner (required copy): `ACTIVITY BECOMES SYNAPSE — ADMITTED INGRESS ONLY`.

## Shell

- One left rail, expanded (~192px). Order: Brain, Explorer, Loom, Sessions, Agents, Code, Knowledge, Delivery, Automations, Observatory, Costs, Settings, Work, Workflows.
- 01 Brain selected. No top product rail.
- Trace-tail mark is static identity. `STREAM LIVE` is a separate status chip, not inside the logo.
- No Tools, Integrations, Policies, Status, Audit, Labs, Stores, Budget, Orphans, Observer, SYSTEM NOMINAL.

## Field

- X = recency buckets: today / week / month / quarter / dormant.
- Project body Y and size = indexed mass (`store_count + artifact_count`).
- Brightness = relative recency.
- Hub exists only when more than one checkout shares `git_common_dir`. Hub id = `repo:<git_common_dir>`. Hub has **no** project mass.

## Live pulse (browser projection only)

`LiveActivityPulse`: `projectId`, `family`, `streamId`, `at`.

This fixture: `projectId=td-core`, `family=ToolCall`, `streamId` must be a shipping stream id (`dashboard_activity` for ToolCall), `at` = observation time. No `units`. No `detail`.

Bloom the exact `projectId`. One hop along a drawn checkout edge to the repo hub at 1/3 energy. Sibling bodies stay dark. Hover is not this plate.

## Typed absence (not shown here)

loading · partial · stale · offline · unavailable · denied · unsupported_schema · measured empty · missing_registry · registry_unavailable. Idle (live + no pulses) is READY, rate 0, climbing age — a different still.

## Remaining still defects

- `streamId: toolcall.main` on the PNG is not a wire stream id.
- Recency rendered as chips; the field itself is the recency axis.
- Background dotted edges are decorative. Only hub→checkout edges exist.
