---
design_status: current
---

# 01 Brain — v10 activity becomes synapse

- **Asset:** `v10-activity-becomes-synapse.png`
- **Lifecycle:** `current`

Lookbook still. CONCEPT / SYNTHETIC. Not a visual-audit golden. Not acceptance.

## Sequence slot

Brain (5): registry idle → project hover → repository zoom → scoped project → **real activity (this plate)**. Current Brain plate.

## Shell

Shared desktop shell with Loom v7: expanded ~192px left rail, TRACEDECAY wordmark, static origin-plus-three-dimmer-dot glyph, same 14 row heights and order, same bottom-strip geometry. 01 Brain selected. No top product rail. STREAM LIVE is a separate chip. No TD-only branding. No SYSTEM NOMINAL.

## Field

Horizontal recency axis TODAY · WEEK · MONTH · QUARTER · DORMANT. Project Y and size = indexed mass. Neural texture inside bodies only. Only line is hub-to-checkout. Hub `repo:/srv/git/td.git` is massless. Exact `projectId` td-core blooms. One hop at 1/3. Siblings dark.

## Pulse

`projectId` td-core · `family` ToolCall · `streamId` dashboard_activity · `at` 2026-08-27T17:50:00Z. No units. No detail.

Banner: ACTIVITY BECOMES SYNAPSE — ADMITTED INGRESS ONLY.

## Intent

Define the current admitted-activity view with measured registry geometry, exact-project bloom, evidenced one-hop propagation, and the shared Brain/Loom shell.

## Entry condition

Open `/brain` after the registry is available and an admitted activity pulse is projected for a known project.

## Visible state

The recency field and indexed-mass project bodies remain measurable while `td-core` blooms, its massless repository hub receives one-third energy, and siblings remain dark.

## Supported interactions

None are executed by this still. Hover, repository zoom, scoped-project content, and scope clearing require their own interaction states.

## Truth boundary

This is a synthetic concept and does not prove a live stream or production interaction. The pictured pulse shape is limited to the browser projection fields named above.

## Lifecycle history

Current Brain plate. It supersedes the pre-Task-1 v4 registry and the corrected v9 intermediate by composing admitted activity with the shared desktop shell used by Loom v7.
