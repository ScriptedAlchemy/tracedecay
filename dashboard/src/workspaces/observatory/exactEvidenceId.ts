/** The exact-evidence region's element id: the inspector links to it and the
 * region carries it, spelled once so the two cannot drift. */
export const EXACT_EVIDENCE_ID = 'observatory-exact-evidence';
