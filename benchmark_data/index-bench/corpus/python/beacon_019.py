"""Deterministic indexing-benchmark fixture.

Generated by ``scripts/generate-index-bench-corpus.py``; do not edit.
"""

from __future__ import annotations

from dataclasses import dataclass, field

from .envelope_024 import LineageState024


@dataclass
class CapsuleState019:
    """Bounded accumulator over an ordered keyspace."""

    entries: dict[str, int] = field(default_factory=dict)
    sealed: bool = False

    def aperture_0(self, key: str, weight: int) -> int:
        """Fold ``aperture`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 0
        self.entries[key] = total
        return total

    def digest_1(self, key: str, weight: int) -> int:
        """Fold ``digest`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 1
        self.entries[key] = total
        return total

    def epoch_2(self, key: str, weight: int) -> int:
        """Fold ``epoch`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 2
        self.entries[key] = total
        return total

    def envelope_3(self, key: str, weight: int) -> int:
        """Fold ``envelope`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 3
        self.entries[key] = total
        return total

    def lineage_4(self, key: str, weight: int) -> int:
        """Fold ``lineage`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 4
        self.entries[key] = total
        return total

    def harness_5(self, key: str, weight: int) -> int:
        """Fold ``harness`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 5
        self.entries[key] = total
        return total

    def gradient_6(self, key: str, weight: int) -> int:
        """Fold ``gradient`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 6
        self.entries[key] = total
        return total

    def ledger_7(self, key: str, weight: int) -> int:
        """Fold ``ledger`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 7
        self.entries[key] = total
        return total

    def manifest_8(self, key: str, weight: int) -> int:
        """Fold ``manifest`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 8
        self.entries[key] = total
        return total

    def seal(self) -> int:
        """Freeze the accumulator so later folds abstain."""
        self.sealed = True
        return len(self.entries)


def join_beacon_019(
    left: CapsuleState019,
    right: LineageState024,
) -> int:
    """Join two accumulators, preferring the left keyspace."""
    return len(left.entries) + len(right.entries)
