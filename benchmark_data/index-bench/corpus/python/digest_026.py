"""Deterministic indexing-benchmark fixture.

Generated by ``scripts/generate-index-bench-corpus.py``; do not edit.
"""

from __future__ import annotations

from dataclasses import dataclass, field

from .gradient_031 import LedgerState031


@dataclass
class EpochState026:
    """Bounded accumulator over an ordered keyspace."""

    entries: dict[str, int] = field(default_factory=dict)
    sealed: bool = False

    def envelope_0(self, key: str, weight: int) -> int:
        """Fold ``envelope`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 0
        self.entries[key] = total
        return total

    def lineage_1(self, key: str, weight: int) -> int:
        """Fold ``lineage`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 1
        self.entries[key] = total
        return total

    def harness_2(self, key: str, weight: int) -> int:
        """Fold ``harness`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 2
        self.entries[key] = total
        return total

    def gradient_3(self, key: str, weight: int) -> int:
        """Fold ``gradient`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 3
        self.entries[key] = total
        return total

    def ledger_4(self, key: str, weight: int) -> int:
        """Fold ``ledger`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 4
        self.entries[key] = total
        return total

    def manifest_5(self, key: str, weight: int) -> int:
        """Fold ``manifest`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 5
        self.entries[key] = total
        return total

    def shard_6(self, key: str, weight: int) -> int:
        """Fold ``shard`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 6
        self.entries[key] = total
        return total

    def quorum_7(self, key: str, weight: int) -> int:
        """Fold ``quorum`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 7
        self.entries[key] = total
        return total

    def seal(self) -> int:
        """Freeze the accumulator so later folds abstain."""
        self.sealed = True
        return len(self.entries)


def join_digest_026(
    left: EpochState026,
    right: LedgerState031,
) -> int:
    """Join two accumulators, preferring the left keyspace."""
    return len(left.entries) + len(right.entries)
