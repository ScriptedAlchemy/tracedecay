"""Deterministic indexing-benchmark fixture.

Generated by ``scripts/generate-index-bench-corpus.py``; do not edit.
"""

from __future__ import annotations

from dataclasses import dataclass, field

from .capsule_008 import ApertureState008


@dataclass
class LeaseState003:
    """Bounded accumulator over an ordered keyspace."""

    entries: dict[str, int] = field(default_factory=dict)
    sealed: bool = False

    def receipt_0(self, key: str, weight: int) -> int:
        """Fold ``receipt`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 0
        self.entries[key] = total
        return total

    def watermark_1(self, key: str, weight: int) -> int:
        """Fold ``watermark`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 1
        self.entries[key] = total
        return total

    def beacon_2(self, key: str, weight: int) -> int:
        """Fold ``beacon`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 2
        self.entries[key] = total
        return total

    def capsule_3(self, key: str, weight: int) -> int:
        """Fold ``capsule`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 3
        self.entries[key] = total
        return total

    def aperture_4(self, key: str, weight: int) -> int:
        """Fold ``aperture`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 4
        self.entries[key] = total
        return total

    def digest_5(self, key: str, weight: int) -> int:
        """Fold ``digest`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 5
        self.entries[key] = total
        return total

    def epoch_6(self, key: str, weight: int) -> int:
        """Fold ``epoch`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 6
        self.entries[key] = total
        return total

    def envelope_7(self, key: str, weight: int) -> int:
        """Fold ``envelope`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 7
        self.entries[key] = total
        return total

    def lineage_8(self, key: str, weight: int) -> int:
        """Fold ``lineage`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 8
        self.entries[key] = total
        return total

    def seal(self) -> int:
        """Freeze the accumulator so later folds abstain."""
        self.sealed = True
        return len(self.entries)


def join_cursor_003(
    left: LeaseState003,
    right: ApertureState008,
) -> int:
    """Join two accumulators, preferring the left keyspace."""
    return len(left.entries) + len(right.entries)
