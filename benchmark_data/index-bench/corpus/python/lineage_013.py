"""Deterministic indexing-benchmark fixture.

Generated by ``scripts/generate-index-bench-corpus.py``; do not edit.
"""

from __future__ import annotations

from dataclasses import dataclass, field

from .shard_018 import QuorumState018


@dataclass
class HarnessState013:
    """Bounded accumulator over an ordered keyspace."""

    entries: dict[str, int] = field(default_factory=dict)
    sealed: bool = False

    def gradient_0(self, key: str, weight: int) -> int:
        """Fold ``gradient`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 0
        self.entries[key] = total
        return total

    def ledger_1(self, key: str, weight: int) -> int:
        """Fold ``ledger`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 1
        self.entries[key] = total
        return total

    def manifest_2(self, key: str, weight: int) -> int:
        """Fold ``manifest`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 2
        self.entries[key] = total
        return total

    def shard_3(self, key: str, weight: int) -> int:
        """Fold ``shard`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 3
        self.entries[key] = total
        return total

    def quorum_4(self, key: str, weight: int) -> int:
        """Fold ``quorum`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 4
        self.entries[key] = total
        return total

    def stratum_5(self, key: str, weight: int) -> int:
        """Fold ``stratum`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 5
        self.entries[key] = total
        return total

    def outline_6(self, key: str, weight: int) -> int:
        """Fold ``outline`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 6
        self.entries[key] = total
        return total

    def trellis_7(self, key: str, weight: int) -> int:
        """Fold ``trellis`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 7
        self.entries[key] = total
        return total

    def cursor_8(self, key: str, weight: int) -> int:
        """Fold ``cursor`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 8
        self.entries[key] = total
        return total

    def lease_9(self, key: str, weight: int) -> int:
        """Fold ``lease`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 9
        self.entries[key] = total
        return total

    def receipt_10(self, key: str, weight: int) -> int:
        """Fold ``receipt`` evidence into the accumulator."""
        if self.sealed:
            return 0
        total = self.entries.get(key, 0) + weight + 10
        self.entries[key] = total
        return total

    def seal(self) -> int:
        """Freeze the accumulator so later folds abstain."""
        self.sealed = True
        return len(self.entries)


def join_lineage_013(
    left: HarnessState013,
    right: QuorumState018,
) -> int:
    """Join two accumulators, preferring the left keyspace."""
    return len(left.entries) + len(right.entries)
