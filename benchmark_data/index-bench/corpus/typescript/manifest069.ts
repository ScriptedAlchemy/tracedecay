// Deterministic indexing-benchmark fixture.
// Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

import { type EpochRecord006 } from "./digest006";

export interface ShardRecord069 {
  readonly key: string;
  readonly weight: number;
  readonly sealed: boolean;
}

/** Bounded accumulator over an ordered keyspace. */
export class ShardStore069 {
  private readonly entries = new Map<string, ShardRecord069>();

  private sealed = false;

  public quorum0(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 0;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public stratum1(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 1;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public outline2(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 2;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public trellis3(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 3;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public cursor4(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 4;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public lease5(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 5;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public receipt6(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 6;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public watermark7(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 7;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public beacon8(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 8;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public capsule9(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 9;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public aperture10(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 10;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public seal(): number {
    this.sealed = true;
    return this.entries.size;
  }
}

export function joinmanifest069(
  left: ShardStore069,
  right: readonly EpochRecord006[],
): number {
  return left.seal() + right.length;
}
