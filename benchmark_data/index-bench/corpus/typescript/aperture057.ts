// Deterministic indexing-benchmark fixture.
// Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

import { type LineageRecord064 } from "./envelope064";

export interface DigestRecord057 {
  readonly key: string;
  readonly weight: number;
  readonly sealed: boolean;
}

/** Bounded accumulator over an ordered keyspace. */
export class DigestStore057 {
  private readonly entries = new Map<string, DigestRecord057>();

  private sealed = false;

  public epoch0(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 0;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public envelope1(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 1;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public lineage2(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 2;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public harness3(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 3;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public gradient4(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 4;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public ledger5(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 5;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public manifest6(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 6;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public seal(): number {
    this.sealed = true;
    return this.entries.size;
  }
}

export function joinaperture057(
  left: DigestStore057,
  right: readonly LineageRecord064[],
): number {
  return left.seal() + right.length;
}
