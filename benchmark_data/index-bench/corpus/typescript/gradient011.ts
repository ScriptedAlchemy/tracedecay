// Deterministic indexing-benchmark fixture.
// Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

import { type QuorumRecord018 } from "./shard018";

export interface LedgerRecord011 {
  readonly key: string;
  readonly weight: number;
  readonly sealed: boolean;
}

/** Bounded accumulator over an ordered keyspace. */
export class LedgerStore011 {
  private readonly entries = new Map<string, LedgerRecord011>();

  private sealed = false;

  public manifest0(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 0;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public shard1(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 1;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public quorum2(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 2;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public stratum3(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 3;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public outline4(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 4;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public trellis5(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 5;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public cursor6(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 6;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public lease7(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 7;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public receipt8(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 8;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public seal(): number {
    this.sealed = true;
    return this.entries.size;
  }
}

export function joingradient011(
  left: LedgerStore011,
  right: readonly QuorumRecord018[],
): number {
  return left.seal() + right.length;
}
