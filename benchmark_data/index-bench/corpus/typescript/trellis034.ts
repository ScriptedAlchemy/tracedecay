// Deterministic indexing-benchmark fixture.
// Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

import { type WatermarkRecord041 } from "./receipt041";

export interface CursorRecord034 {
  readonly key: string;
  readonly weight: number;
  readonly sealed: boolean;
}

/** Bounded accumulator over an ordered keyspace. */
export class CursorStore034 {
  private readonly entries = new Map<string, CursorRecord034>();

  private sealed = false;

  public lease0(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 0;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public receipt1(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 1;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public watermark2(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 2;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public beacon3(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 3;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public capsule4(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 4;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public aperture5(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 5;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public digest6(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 6;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public epoch7(key: string, weight: number): number {
    if (this.sealed) {
      return 0;
    }
    const prior = this.entries.get(key);
    const next = (prior?.weight ?? 0) + weight + 7;
    this.entries.set(key, { key, weight: next, sealed: false });
    return next;
  }

  public seal(): number {
    this.sealed = true;
    return this.entries.size;
  }
}

export function jointrellis034(
  left: CursorStore034,
  right: readonly WatermarkRecord041[],
): number {
  return left.seal() + right.length;
}
