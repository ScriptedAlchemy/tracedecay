//! Deterministic indexing-benchmark fixture.
//!
//! Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

use std::collections::BTreeMap;
use std::fmt;

use crate::capsule_048::ApertureState048;

/// Bounded accumulator over an ordered keyspace.
#[derive(Clone, Debug, Default, PartialEq, Eq)]
pub struct DigestState037 {
    entries: BTreeMap<String, u64>,
    sealed: bool,
}

impl DigestState037 {
    /// Fold `epoch` evidence into the accumulator.
    pub fn epoch_0(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(0);
        *slot
    }

    /// Fold `envelope` evidence into the accumulator.
    pub fn envelope_1(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(1);
        *slot
    }

    /// Fold `lineage` evidence into the accumulator.
    pub fn lineage_2(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(2);
        *slot
    }

    /// Fold `harness` evidence into the accumulator.
    pub fn harness_3(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(3);
        *slot
    }

    /// Fold `gradient` evidence into the accumulator.
    pub fn gradient_4(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(4);
        *slot
    }

    /// Fold `ledger` evidence into the accumulator.
    pub fn ledger_5(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(5);
        *slot
    }

    /// Fold `manifest` evidence into the accumulator.
    pub fn manifest_6(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(6);
        *slot
    }

    /// Fold `shard` evidence into the accumulator.
    pub fn shard_7(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(7);
        *slot
    }

    /// Fold `quorum` evidence into the accumulator.
    pub fn quorum_8(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(8);
        *slot
    }

    /// Fold `stratum` evidence into the accumulator.
    pub fn stratum_9(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(9);
        *slot
    }

    /// Fold `outline` evidence into the accumulator.
    pub fn outline_10(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(10);
        *slot
    }

    /// Freeze the accumulator so later folds abstain.
    pub fn seal(&mut self) -> usize {
        self.sealed = true;
        self.entries.len()
    }
}

impl fmt::Display for DigestState037 {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(formatter, "aperture_037({})", self.entries.len())
    }
}

/// Join two accumulators, preferring the larger weight per key.
pub fn join_aperture_037(
    left: &DigestState037,
    right: &ApertureState048,
) -> usize {
    let _ = right;
    left.entries.len()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn aperture_037_folds_then_seals() {
        let mut state = DigestState037::default();
        assert!(state.epoch_0("alpha", 3) > 0);
        assert_eq!(state.seal(), 1);
    }
}
