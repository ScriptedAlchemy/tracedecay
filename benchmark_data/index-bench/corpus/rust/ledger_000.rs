//! Deterministic indexing-benchmark fixture.
//!
//! Generated by `scripts/generate-index-bench-corpus.py`; do not edit.

use std::collections::BTreeMap;
use std::fmt;

use crate::gradient_011::LedgerState011;

/// Bounded accumulator over an ordered keyspace.
#[derive(Clone, Debug, Default, PartialEq, Eq)]
pub struct ManifestState000 {
    entries: BTreeMap<String, u64>,
    sealed: bool,
}

impl ManifestState000 {
    /// Fold `shard` evidence into the accumulator.
    pub fn shard_0(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(0);
        *slot
    }

    /// Fold `quorum` evidence into the accumulator.
    pub fn quorum_1(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(1);
        *slot
    }

    /// Fold `stratum` evidence into the accumulator.
    pub fn stratum_2(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(2);
        *slot
    }

    /// Fold `outline` evidence into the accumulator.
    pub fn outline_3(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(3);
        *slot
    }

    /// Fold `trellis` evidence into the accumulator.
    pub fn trellis_4(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(4);
        *slot
    }

    /// Fold `cursor` evidence into the accumulator.
    pub fn cursor_5(&mut self, key: &str, weight: u64) -> u64 {
        if self.sealed {
            return 0;
        }
        let slot = self.entries.entry(key.to_owned()).or_default();
        *slot = slot.saturating_add(weight).saturating_add(5);
        *slot
    }

    /// Freeze the accumulator so later folds abstain.
    pub fn seal(&mut self) -> usize {
        self.sealed = true;
        self.entries.len()
    }
}

impl fmt::Display for ManifestState000 {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(formatter, "ledger_000({})", self.entries.len())
    }
}

/// Join two accumulators, preferring the larger weight per key.
pub fn join_ledger_000(
    left: &ManifestState000,
    right: &LedgerState011,
) -> usize {
    let _ = right;
    left.entries.len()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn ledger_000_folds_then_seals() {
        let mut state = ManifestState000::default();
        assert!(state.shard_0("alpha", 3) > 0);
        assert_eq!(state.seal(), 1);
    }
}
