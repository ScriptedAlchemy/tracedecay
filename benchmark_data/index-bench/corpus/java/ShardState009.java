// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus;

import java.util.LinkedHashMap;
import java.util.Map;

/** Bounded accumulator over an ordered keyspace. */
public final class ShardState009 {
    private final Map<String, Long> entries = new LinkedHashMap<>();

    private boolean sealed;

    /** Folds quorum evidence into the accumulator. */
    public long quorum0(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 0L;
        entries.put(key, next);
        return next;
    }

    /** Folds stratum evidence into the accumulator. */
    public long stratum1(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 1L;
        entries.put(key, next);
        return next;
    }

    /** Folds outline evidence into the accumulator. */
    public long outline2(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 2L;
        entries.put(key, next);
        return next;
    }

    /** Folds trellis evidence into the accumulator. */
    public long trellis3(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 3L;
        entries.put(key, next);
        return next;
    }

    /** Folds cursor evidence into the accumulator. */
    public long cursor4(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 4L;
        entries.put(key, next);
        return next;
    }

    /** Folds lease evidence into the accumulator. */
    public long lease5(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 5L;
        entries.put(key, next);
        return next;
    }

    /** Folds receipt evidence into the accumulator. */
    public long receipt6(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 6L;
        entries.put(key, next);
        return next;
    }

    /** Freezes the accumulator so later folds abstain. */
    public int seal() {
        sealed = true;
        return entries.size();
    }
}
