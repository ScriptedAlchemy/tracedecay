// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus;

import java.util.LinkedHashMap;
import java.util.Map;

/** Bounded accumulator over an ordered keyspace. */
public final class CapsuleState019 {
    private final Map<String, Long> entries = new LinkedHashMap<>();

    private boolean sealed;

    /** Folds aperture evidence into the accumulator. */
    public long aperture0(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 0L;
        entries.put(key, next);
        return next;
    }

    /** Folds digest evidence into the accumulator. */
    public long digest1(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 1L;
        entries.put(key, next);
        return next;
    }

    /** Folds epoch evidence into the accumulator. */
    public long epoch2(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 2L;
        entries.put(key, next);
        return next;
    }

    /** Folds envelope evidence into the accumulator. */
    public long envelope3(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 3L;
        entries.put(key, next);
        return next;
    }

    /** Folds lineage evidence into the accumulator. */
    public long lineage4(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 4L;
        entries.put(key, next);
        return next;
    }

    /** Folds harness evidence into the accumulator. */
    public long harness5(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 5L;
        entries.put(key, next);
        return next;
    }

    /** Folds gradient evidence into the accumulator. */
    public long gradient6(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 6L;
        entries.put(key, next);
        return next;
    }

    /** Folds ledger evidence into the accumulator. */
    public long ledger7(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 7L;
        entries.put(key, next);
        return next;
    }

    /** Folds manifest evidence into the accumulator. */
    public long manifest8(String key, long weight) {
        if (sealed) {
            return 0L;
        }
        long next = entries.getOrDefault(key, 0L) + weight + 8L;
        entries.put(key, next);
        return next;
    }

    /** Freezes the accumulator so later folds abstain. */
    public int seal() {
        sealed = true;
        return entries.size();
    }
}
