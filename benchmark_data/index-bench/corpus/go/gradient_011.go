// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Gradient011State struct {
	entries map[string]uint64
	sealed  bool
}

// NewGradient011State returns an empty accumulator.
func NewGradient011State() *Gradient011State {
	return &Gradient011State{entries: map[string]uint64{}}
}

// Manifest0 folds evidence into the accumulator.
func (s *Gradient011State) Manifest0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Shard1 folds evidence into the accumulator.
func (s *Gradient011State) Shard1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Quorum2 folds evidence into the accumulator.
func (s *Gradient011State) Quorum2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Stratum3 folds evidence into the accumulator.
func (s *Gradient011State) Stratum3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Outline4 folds evidence into the accumulator.
func (s *Gradient011State) Outline4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Trellis5 folds evidence into the accumulator.
func (s *Gradient011State) Trellis5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Cursor6 folds evidence into the accumulator.
func (s *Gradient011State) Cursor6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Lease7 folds evidence into the accumulator.
func (s *Gradient011State) Lease7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Receipt8 folds evidence into the accumulator.
func (s *Gradient011State) Receipt8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Gradient011State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Gradient011State) String() string {
	return fmt.Sprintf("Gradient011(%d)", len(s.entries))
}
