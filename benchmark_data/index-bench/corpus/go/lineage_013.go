// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Lineage013State struct {
	entries map[string]uint64
	sealed  bool
}

// NewLineage013State returns an empty accumulator.
func NewLineage013State() *Lineage013State {
	return &Lineage013State{entries: map[string]uint64{}}
}

// Gradient0 folds evidence into the accumulator.
func (s *Lineage013State) Gradient0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Ledger1 folds evidence into the accumulator.
func (s *Lineage013State) Ledger1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Manifest2 folds evidence into the accumulator.
func (s *Lineage013State) Manifest2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Shard3 folds evidence into the accumulator.
func (s *Lineage013State) Shard3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Quorum4 folds evidence into the accumulator.
func (s *Lineage013State) Quorum4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Stratum5 folds evidence into the accumulator.
func (s *Lineage013State) Stratum5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Outline6 folds evidence into the accumulator.
func (s *Lineage013State) Outline6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Trellis7 folds evidence into the accumulator.
func (s *Lineage013State) Trellis7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Cursor8 folds evidence into the accumulator.
func (s *Lineage013State) Cursor8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Lease9 folds evidence into the accumulator.
func (s *Lineage013State) Lease9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Receipt10 folds evidence into the accumulator.
func (s *Lineage013State) Receipt10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Lineage013State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Lineage013State) String() string {
	return fmt.Sprintf("Lineage013(%d)", len(s.entries))
}
