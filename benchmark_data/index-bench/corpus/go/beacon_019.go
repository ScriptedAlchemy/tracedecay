// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Beacon019State struct {
	entries map[string]uint64
	sealed  bool
}

// NewBeacon019State returns an empty accumulator.
func NewBeacon019State() *Beacon019State {
	return &Beacon019State{entries: map[string]uint64{}}
}

// Aperture0 folds evidence into the accumulator.
func (s *Beacon019State) Aperture0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Digest1 folds evidence into the accumulator.
func (s *Beacon019State) Digest1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Epoch2 folds evidence into the accumulator.
func (s *Beacon019State) Epoch2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Envelope3 folds evidence into the accumulator.
func (s *Beacon019State) Envelope3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Lineage4 folds evidence into the accumulator.
func (s *Beacon019State) Lineage4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Harness5 folds evidence into the accumulator.
func (s *Beacon019State) Harness5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Gradient6 folds evidence into the accumulator.
func (s *Beacon019State) Gradient6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Ledger7 folds evidence into the accumulator.
func (s *Beacon019State) Ledger7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Manifest8 folds evidence into the accumulator.
func (s *Beacon019State) Manifest8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Beacon019State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Beacon019State) String() string {
	return fmt.Sprintf("Beacon019(%d)", len(s.entries))
}
