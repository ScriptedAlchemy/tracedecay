// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Envelope004State struct {
	entries map[string]uint64
	sealed  bool
}

// NewEnvelope004State returns an empty accumulator.
func NewEnvelope004State() *Envelope004State {
	return &Envelope004State{entries: map[string]uint64{}}
}

// Harness0 folds evidence into the accumulator.
func (s *Envelope004State) Harness0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Gradient1 folds evidence into the accumulator.
func (s *Envelope004State) Gradient1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Ledger2 folds evidence into the accumulator.
func (s *Envelope004State) Ledger2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Manifest3 folds evidence into the accumulator.
func (s *Envelope004State) Manifest3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Shard4 folds evidence into the accumulator.
func (s *Envelope004State) Shard4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Quorum5 folds evidence into the accumulator.
func (s *Envelope004State) Quorum5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Stratum6 folds evidence into the accumulator.
func (s *Envelope004State) Stratum6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Outline7 folds evidence into the accumulator.
func (s *Envelope004State) Outline7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Trellis8 folds evidence into the accumulator.
func (s *Envelope004State) Trellis8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Cursor9 folds evidence into the accumulator.
func (s *Envelope004State) Cursor9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Envelope004State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Envelope004State) String() string {
	return fmt.Sprintf("Envelope004(%d)", len(s.entries))
}
