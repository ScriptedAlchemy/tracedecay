// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Ledger020State struct {
	entries map[string]uint64
	sealed  bool
}

// NewLedger020State returns an empty accumulator.
func NewLedger020State() *Ledger020State {
	return &Ledger020State{entries: map[string]uint64{}}
}

// Shard0 folds evidence into the accumulator.
func (s *Ledger020State) Shard0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Quorum1 folds evidence into the accumulator.
func (s *Ledger020State) Quorum1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Stratum2 folds evidence into the accumulator.
func (s *Ledger020State) Stratum2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Outline3 folds evidence into the accumulator.
func (s *Ledger020State) Outline3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Trellis4 folds evidence into the accumulator.
func (s *Ledger020State) Trellis4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Cursor5 folds evidence into the accumulator.
func (s *Ledger020State) Cursor5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Lease6 folds evidence into the accumulator.
func (s *Ledger020State) Lease6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Receipt7 folds evidence into the accumulator.
func (s *Ledger020State) Receipt7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Watermark8 folds evidence into the accumulator.
func (s *Ledger020State) Watermark8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Beacon9 folds evidence into the accumulator.
func (s *Ledger020State) Beacon9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Ledger020State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Ledger020State) String() string {
	return fmt.Sprintf("Ledger020(%d)", len(s.entries))
}
