// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Trellis014State struct {
	entries map[string]uint64
	sealed  bool
}

// NewTrellis014State returns an empty accumulator.
func NewTrellis014State() *Trellis014State {
	return &Trellis014State{entries: map[string]uint64{}}
}

// Lease0 folds evidence into the accumulator.
func (s *Trellis014State) Lease0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Receipt1 folds evidence into the accumulator.
func (s *Trellis014State) Receipt1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Watermark2 folds evidence into the accumulator.
func (s *Trellis014State) Watermark2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Beacon3 folds evidence into the accumulator.
func (s *Trellis014State) Beacon3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Capsule4 folds evidence into the accumulator.
func (s *Trellis014State) Capsule4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Aperture5 folds evidence into the accumulator.
func (s *Trellis014State) Aperture5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Digest6 folds evidence into the accumulator.
func (s *Trellis014State) Digest6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Epoch7 folds evidence into the accumulator.
func (s *Trellis014State) Epoch7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Envelope8 folds evidence into the accumulator.
func (s *Trellis014State) Envelope8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Lineage9 folds evidence into the accumulator.
func (s *Trellis014State) Lineage9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Harness10 folds evidence into the accumulator.
func (s *Trellis014State) Harness10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Gradient11 folds evidence into the accumulator.
func (s *Trellis014State) Gradient11(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 11
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Trellis014State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Trellis014State) String() string {
	return fmt.Sprintf("Trellis014(%d)", len(s.entries))
}
