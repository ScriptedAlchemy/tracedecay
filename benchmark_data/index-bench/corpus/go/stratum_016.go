// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Stratum016State struct {
	entries map[string]uint64
	sealed  bool
}

// NewStratum016State returns an empty accumulator.
func NewStratum016State() *Stratum016State {
	return &Stratum016State{entries: map[string]uint64{}}
}

// Trellis0 folds evidence into the accumulator.
func (s *Stratum016State) Trellis0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Cursor1 folds evidence into the accumulator.
func (s *Stratum016State) Cursor1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Lease2 folds evidence into the accumulator.
func (s *Stratum016State) Lease2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Receipt3 folds evidence into the accumulator.
func (s *Stratum016State) Receipt3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Watermark4 folds evidence into the accumulator.
func (s *Stratum016State) Watermark4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Beacon5 folds evidence into the accumulator.
func (s *Stratum016State) Beacon5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Stratum016State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Stratum016State) String() string {
	return fmt.Sprintf("Stratum016(%d)", len(s.entries))
}
