// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Outline025State struct {
	entries map[string]uint64
	sealed  bool
}

// NewOutline025State returns an empty accumulator.
func NewOutline025State() *Outline025State {
	return &Outline025State{entries: map[string]uint64{}}
}

// Cursor0 folds evidence into the accumulator.
func (s *Outline025State) Cursor0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Lease1 folds evidence into the accumulator.
func (s *Outline025State) Lease1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Receipt2 folds evidence into the accumulator.
func (s *Outline025State) Receipt2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Watermark3 folds evidence into the accumulator.
func (s *Outline025State) Watermark3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Beacon4 folds evidence into the accumulator.
func (s *Outline025State) Beacon4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Capsule5 folds evidence into the accumulator.
func (s *Outline025State) Capsule5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Aperture6 folds evidence into the accumulator.
func (s *Outline025State) Aperture6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Outline025State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Outline025State) String() string {
	return fmt.Sprintf("Outline025(%d)", len(s.entries))
}
