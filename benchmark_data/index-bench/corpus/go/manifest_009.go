// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Manifest009State struct {
	entries map[string]uint64
	sealed  bool
}

// NewManifest009State returns an empty accumulator.
func NewManifest009State() *Manifest009State {
	return &Manifest009State{entries: map[string]uint64{}}
}

// Quorum0 folds evidence into the accumulator.
func (s *Manifest009State) Quorum0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Stratum1 folds evidence into the accumulator.
func (s *Manifest009State) Stratum1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Outline2 folds evidence into the accumulator.
func (s *Manifest009State) Outline2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Trellis3 folds evidence into the accumulator.
func (s *Manifest009State) Trellis3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Cursor4 folds evidence into the accumulator.
func (s *Manifest009State) Cursor4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Lease5 folds evidence into the accumulator.
func (s *Manifest009State) Lease5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Receipt6 folds evidence into the accumulator.
func (s *Manifest009State) Receipt6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Manifest009State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Manifest009State) String() string {
	return fmt.Sprintf("Manifest009(%d)", len(s.entries))
}
