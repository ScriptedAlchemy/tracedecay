// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Quorum007State struct {
	entries map[string]uint64
	sealed  bool
}

// NewQuorum007State returns an empty accumulator.
func NewQuorum007State() *Quorum007State {
	return &Quorum007State{entries: map[string]uint64{}}
}

// Outline0 folds evidence into the accumulator.
func (s *Quorum007State) Outline0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Trellis1 folds evidence into the accumulator.
func (s *Quorum007State) Trellis1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Cursor2 folds evidence into the accumulator.
func (s *Quorum007State) Cursor2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Lease3 folds evidence into the accumulator.
func (s *Quorum007State) Lease3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Receipt4 folds evidence into the accumulator.
func (s *Quorum007State) Receipt4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Watermark5 folds evidence into the accumulator.
func (s *Quorum007State) Watermark5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Beacon6 folds evidence into the accumulator.
func (s *Quorum007State) Beacon6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Capsule7 folds evidence into the accumulator.
func (s *Quorum007State) Capsule7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Aperture8 folds evidence into the accumulator.
func (s *Quorum007State) Aperture8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Digest9 folds evidence into the accumulator.
func (s *Quorum007State) Digest9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Epoch10 folds evidence into the accumulator.
func (s *Quorum007State) Epoch10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Envelope11 folds evidence into the accumulator.
func (s *Quorum007State) Envelope11(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 11
	return s.entries[key]
}

// Lineage12 folds evidence into the accumulator.
func (s *Quorum007State) Lineage12(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 12
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Quorum007State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Quorum007State) String() string {
	return fmt.Sprintf("Quorum007(%d)", len(s.entries))
}
