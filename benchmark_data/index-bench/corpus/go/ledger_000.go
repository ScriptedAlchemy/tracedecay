// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Ledger000State struct {
	entries map[string]uint64
	sealed  bool
}

// NewLedger000State returns an empty accumulator.
func NewLedger000State() *Ledger000State {
	return &Ledger000State{entries: map[string]uint64{}}
}

// Shard0 folds evidence into the accumulator.
func (s *Ledger000State) Shard0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Quorum1 folds evidence into the accumulator.
func (s *Ledger000State) Quorum1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Stratum2 folds evidence into the accumulator.
func (s *Ledger000State) Stratum2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Outline3 folds evidence into the accumulator.
func (s *Ledger000State) Outline3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Trellis4 folds evidence into the accumulator.
func (s *Ledger000State) Trellis4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Cursor5 folds evidence into the accumulator.
func (s *Ledger000State) Cursor5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Ledger000State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Ledger000State) String() string {
	return fmt.Sprintf("Ledger000(%d)", len(s.entries))
}
