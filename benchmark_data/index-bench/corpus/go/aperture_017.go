// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Aperture017State struct {
	entries map[string]uint64
	sealed  bool
}

// NewAperture017State returns an empty accumulator.
func NewAperture017State() *Aperture017State {
	return &Aperture017State{entries: map[string]uint64{}}
}

// Epoch0 folds evidence into the accumulator.
func (s *Aperture017State) Epoch0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Envelope1 folds evidence into the accumulator.
func (s *Aperture017State) Envelope1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Lineage2 folds evidence into the accumulator.
func (s *Aperture017State) Lineage2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Harness3 folds evidence into the accumulator.
func (s *Aperture017State) Harness3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Gradient4 folds evidence into the accumulator.
func (s *Aperture017State) Gradient4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Ledger5 folds evidence into the accumulator.
func (s *Aperture017State) Ledger5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Manifest6 folds evidence into the accumulator.
func (s *Aperture017State) Manifest6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Aperture017State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Aperture017State) String() string {
	return fmt.Sprintf("Aperture017(%d)", len(s.entries))
}
