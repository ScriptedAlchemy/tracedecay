// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Shard018State struct {
	entries map[string]uint64
	sealed  bool
}

// NewShard018State returns an empty accumulator.
func NewShard018State() *Shard018State {
	return &Shard018State{entries: map[string]uint64{}}
}

// Stratum0 folds evidence into the accumulator.
func (s *Shard018State) Stratum0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Outline1 folds evidence into the accumulator.
func (s *Shard018State) Outline1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Trellis2 folds evidence into the accumulator.
func (s *Shard018State) Trellis2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Cursor3 folds evidence into the accumulator.
func (s *Shard018State) Cursor3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Lease4 folds evidence into the accumulator.
func (s *Shard018State) Lease4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Receipt5 folds evidence into the accumulator.
func (s *Shard018State) Receipt5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Watermark6 folds evidence into the accumulator.
func (s *Shard018State) Watermark6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Beacon7 folds evidence into the accumulator.
func (s *Shard018State) Beacon7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Shard018State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Shard018State) String() string {
	return fmt.Sprintf("Shard018(%d)", len(s.entries))
}
