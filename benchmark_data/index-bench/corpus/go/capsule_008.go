// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Capsule008State struct {
	entries map[string]uint64
	sealed  bool
}

// NewCapsule008State returns an empty accumulator.
func NewCapsule008State() *Capsule008State {
	return &Capsule008State{entries: map[string]uint64{}}
}

// Digest0 folds evidence into the accumulator.
func (s *Capsule008State) Digest0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Epoch1 folds evidence into the accumulator.
func (s *Capsule008State) Epoch1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Envelope2 folds evidence into the accumulator.
func (s *Capsule008State) Envelope2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Lineage3 folds evidence into the accumulator.
func (s *Capsule008State) Lineage3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Harness4 folds evidence into the accumulator.
func (s *Capsule008State) Harness4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Gradient5 folds evidence into the accumulator.
func (s *Capsule008State) Gradient5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Capsule008State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Capsule008State) String() string {
	return fmt.Sprintf("Capsule008(%d)", len(s.entries))
}
