// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Epoch015State struct {
	entries map[string]uint64
	sealed  bool
}

// NewEpoch015State returns an empty accumulator.
func NewEpoch015State() *Epoch015State {
	return &Epoch015State{entries: map[string]uint64{}}
}

// Lineage0 folds evidence into the accumulator.
func (s *Epoch015State) Lineage0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Harness1 folds evidence into the accumulator.
func (s *Epoch015State) Harness1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Gradient2 folds evidence into the accumulator.
func (s *Epoch015State) Gradient2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Ledger3 folds evidence into the accumulator.
func (s *Epoch015State) Ledger3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Manifest4 folds evidence into the accumulator.
func (s *Epoch015State) Manifest4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Shard5 folds evidence into the accumulator.
func (s *Epoch015State) Shard5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Quorum6 folds evidence into the accumulator.
func (s *Epoch015State) Quorum6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Stratum7 folds evidence into the accumulator.
func (s *Epoch015State) Stratum7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Outline8 folds evidence into the accumulator.
func (s *Epoch015State) Outline8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Trellis9 folds evidence into the accumulator.
func (s *Epoch015State) Trellis9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Cursor10 folds evidence into the accumulator.
func (s *Epoch015State) Cursor10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Lease11 folds evidence into the accumulator.
func (s *Epoch015State) Lease11(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 11
	return s.entries[key]
}

// Receipt12 folds evidence into the accumulator.
func (s *Epoch015State) Receipt12(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 12
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Epoch015State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Epoch015State) String() string {
	return fmt.Sprintf("Epoch015(%d)", len(s.entries))
}
