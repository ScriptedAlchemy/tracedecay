// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Cursor003State struct {
	entries map[string]uint64
	sealed  bool
}

// NewCursor003State returns an empty accumulator.
func NewCursor003State() *Cursor003State {
	return &Cursor003State{entries: map[string]uint64{}}
}

// Receipt0 folds evidence into the accumulator.
func (s *Cursor003State) Receipt0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Watermark1 folds evidence into the accumulator.
func (s *Cursor003State) Watermark1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Beacon2 folds evidence into the accumulator.
func (s *Cursor003State) Beacon2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Capsule3 folds evidence into the accumulator.
func (s *Cursor003State) Capsule3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Aperture4 folds evidence into the accumulator.
func (s *Cursor003State) Aperture4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Digest5 folds evidence into the accumulator.
func (s *Cursor003State) Digest5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Epoch6 folds evidence into the accumulator.
func (s *Cursor003State) Epoch6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Envelope7 folds evidence into the accumulator.
func (s *Cursor003State) Envelope7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Lineage8 folds evidence into the accumulator.
func (s *Cursor003State) Lineage8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Cursor003State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Cursor003State) String() string {
	return fmt.Sprintf("Cursor003(%d)", len(s.entries))
}
