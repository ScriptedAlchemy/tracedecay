// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Capsule028State struct {
	entries map[string]uint64
	sealed  bool
}

// NewCapsule028State returns an empty accumulator.
func NewCapsule028State() *Capsule028State {
	return &Capsule028State{entries: map[string]uint64{}}
}

// Digest0 folds evidence into the accumulator.
func (s *Capsule028State) Digest0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Epoch1 folds evidence into the accumulator.
func (s *Capsule028State) Epoch1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Envelope2 folds evidence into the accumulator.
func (s *Capsule028State) Envelope2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Lineage3 folds evidence into the accumulator.
func (s *Capsule028State) Lineage3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Harness4 folds evidence into the accumulator.
func (s *Capsule028State) Harness4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Gradient5 folds evidence into the accumulator.
func (s *Capsule028State) Gradient5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Ledger6 folds evidence into the accumulator.
func (s *Capsule028State) Ledger6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Manifest7 folds evidence into the accumulator.
func (s *Capsule028State) Manifest7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Shard8 folds evidence into the accumulator.
func (s *Capsule028State) Shard8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Quorum9 folds evidence into the accumulator.
func (s *Capsule028State) Quorum9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Capsule028State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Capsule028State) String() string {
	return fmt.Sprintf("Capsule028(%d)", len(s.entries))
}
