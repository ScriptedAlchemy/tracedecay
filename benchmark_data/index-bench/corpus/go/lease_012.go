// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Lease012State struct {
	entries map[string]uint64
	sealed  bool
}

// NewLease012State returns an empty accumulator.
func NewLease012State() *Lease012State {
	return &Lease012State{entries: map[string]uint64{}}
}

// Watermark0 folds evidence into the accumulator.
func (s *Lease012State) Watermark0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Beacon1 folds evidence into the accumulator.
func (s *Lease012State) Beacon1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Capsule2 folds evidence into the accumulator.
func (s *Lease012State) Capsule2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Aperture3 folds evidence into the accumulator.
func (s *Lease012State) Aperture3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Digest4 folds evidence into the accumulator.
func (s *Lease012State) Digest4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Epoch5 folds evidence into the accumulator.
func (s *Lease012State) Epoch5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Envelope6 folds evidence into the accumulator.
func (s *Lease012State) Envelope6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Lineage7 folds evidence into the accumulator.
func (s *Lease012State) Lineage7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Harness8 folds evidence into the accumulator.
func (s *Lease012State) Harness8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Gradient9 folds evidence into the accumulator.
func (s *Lease012State) Gradient9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Lease012State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Lease012State) String() string {
	return fmt.Sprintf("Lease012(%d)", len(s.entries))
}
