// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Harness022State struct {
	entries map[string]uint64
	sealed  bool
}

// NewHarness022State returns an empty accumulator.
func NewHarness022State() *Harness022State {
	return &Harness022State{entries: map[string]uint64{}}
}

// Ledger0 folds evidence into the accumulator.
func (s *Harness022State) Ledger0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Manifest1 folds evidence into the accumulator.
func (s *Harness022State) Manifest1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Shard2 folds evidence into the accumulator.
func (s *Harness022State) Shard2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Quorum3 folds evidence into the accumulator.
func (s *Harness022State) Quorum3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Stratum4 folds evidence into the accumulator.
func (s *Harness022State) Stratum4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Outline5 folds evidence into the accumulator.
func (s *Harness022State) Outline5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Trellis6 folds evidence into the accumulator.
func (s *Harness022State) Trellis6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Cursor7 folds evidence into the accumulator.
func (s *Harness022State) Cursor7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Lease8 folds evidence into the accumulator.
func (s *Harness022State) Lease8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Receipt9 folds evidence into the accumulator.
func (s *Harness022State) Receipt9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Watermark10 folds evidence into the accumulator.
func (s *Harness022State) Watermark10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Beacon11 folds evidence into the accumulator.
func (s *Harness022State) Beacon11(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 11
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Harness022State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Harness022State) String() string {
	return fmt.Sprintf("Harness022(%d)", len(s.entries))
}
