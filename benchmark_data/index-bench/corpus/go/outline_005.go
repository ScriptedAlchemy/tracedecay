// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Outline005State struct {
	entries map[string]uint64
	sealed  bool
}

// NewOutline005State returns an empty accumulator.
func NewOutline005State() *Outline005State {
	return &Outline005State{entries: map[string]uint64{}}
}

// Cursor0 folds evidence into the accumulator.
func (s *Outline005State) Cursor0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Lease1 folds evidence into the accumulator.
func (s *Outline005State) Lease1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Receipt2 folds evidence into the accumulator.
func (s *Outline005State) Receipt2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Watermark3 folds evidence into the accumulator.
func (s *Outline005State) Watermark3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Beacon4 folds evidence into the accumulator.
func (s *Outline005State) Beacon4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Capsule5 folds evidence into the accumulator.
func (s *Outline005State) Capsule5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Aperture6 folds evidence into the accumulator.
func (s *Outline005State) Aperture6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Digest7 folds evidence into the accumulator.
func (s *Outline005State) Digest7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Epoch8 folds evidence into the accumulator.
func (s *Outline005State) Epoch8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Envelope9 folds evidence into the accumulator.
func (s *Outline005State) Envelope9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Lineage10 folds evidence into the accumulator.
func (s *Outline005State) Lineage10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Outline005State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Outline005State) String() string {
	return fmt.Sprintf("Outline005(%d)", len(s.entries))
}
