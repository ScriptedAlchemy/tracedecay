// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Cursor023State struct {
	entries map[string]uint64
	sealed  bool
}

// NewCursor023State returns an empty accumulator.
func NewCursor023State() *Cursor023State {
	return &Cursor023State{entries: map[string]uint64{}}
}

// Receipt0 folds evidence into the accumulator.
func (s *Cursor023State) Receipt0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Watermark1 folds evidence into the accumulator.
func (s *Cursor023State) Watermark1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Beacon2 folds evidence into the accumulator.
func (s *Cursor023State) Beacon2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Capsule3 folds evidence into the accumulator.
func (s *Cursor023State) Capsule3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Aperture4 folds evidence into the accumulator.
func (s *Cursor023State) Aperture4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Digest5 folds evidence into the accumulator.
func (s *Cursor023State) Digest5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Epoch6 folds evidence into the accumulator.
func (s *Cursor023State) Epoch6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Envelope7 folds evidence into the accumulator.
func (s *Cursor023State) Envelope7(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 7
	return s.entries[key]
}

// Lineage8 folds evidence into the accumulator.
func (s *Cursor023State) Lineage8(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 8
	return s.entries[key]
}

// Harness9 folds evidence into the accumulator.
func (s *Cursor023State) Harness9(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 9
	return s.entries[key]
}

// Gradient10 folds evidence into the accumulator.
func (s *Cursor023State) Gradient10(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 10
	return s.entries[key]
}

// Ledger11 folds evidence into the accumulator.
func (s *Cursor023State) Ledger11(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 11
	return s.entries[key]
}

// Manifest12 folds evidence into the accumulator.
func (s *Cursor023State) Manifest12(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 12
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Cursor023State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Cursor023State) String() string {
	return fmt.Sprintf("Cursor023(%d)", len(s.entries))
}
