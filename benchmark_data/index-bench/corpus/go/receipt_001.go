// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Receipt001State struct {
	entries map[string]uint64
	sealed  bool
}

// NewReceipt001State returns an empty accumulator.
func NewReceipt001State() *Receipt001State {
	return &Receipt001State{entries: map[string]uint64{}}
}

// Beacon0 folds evidence into the accumulator.
func (s *Receipt001State) Beacon0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Capsule1 folds evidence into the accumulator.
func (s *Receipt001State) Capsule1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Aperture2 folds evidence into the accumulator.
func (s *Receipt001State) Aperture2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Digest3 folds evidence into the accumulator.
func (s *Receipt001State) Digest3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Epoch4 folds evidence into the accumulator.
func (s *Receipt001State) Epoch4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Envelope5 folds evidence into the accumulator.
func (s *Receipt001State) Envelope5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Lineage6 folds evidence into the accumulator.
func (s *Receipt001State) Lineage6(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 6
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Receipt001State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Receipt001State) String() string {
	return fmt.Sprintf("Receipt001(%d)", len(s.entries))
}
