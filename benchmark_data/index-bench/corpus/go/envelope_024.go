// Deterministic indexing-benchmark fixture.
// Generated by scripts/generate-index-bench-corpus.py; do not edit.

package corpus

import "fmt"

// State is a bounded accumulator over an ordered keyspace.
type Envelope024State struct {
	entries map[string]uint64
	sealed  bool
}

// NewEnvelope024State returns an empty accumulator.
func NewEnvelope024State() *Envelope024State {
	return &Envelope024State{entries: map[string]uint64{}}
}

// Harness0 folds evidence into the accumulator.
func (s *Envelope024State) Harness0(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 0
	return s.entries[key]
}

// Gradient1 folds evidence into the accumulator.
func (s *Envelope024State) Gradient1(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 1
	return s.entries[key]
}

// Ledger2 folds evidence into the accumulator.
func (s *Envelope024State) Ledger2(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 2
	return s.entries[key]
}

// Manifest3 folds evidence into the accumulator.
func (s *Envelope024State) Manifest3(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 3
	return s.entries[key]
}

// Shard4 folds evidence into the accumulator.
func (s *Envelope024State) Shard4(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 4
	return s.entries[key]
}

// Quorum5 folds evidence into the accumulator.
func (s *Envelope024State) Quorum5(key string, weight uint64) uint64 {
	if s.sealed {
		return 0
	}
	s.entries[key] += weight + 5
	return s.entries[key]
}

// Seal freezes the accumulator so later folds abstain.
func (s *Envelope024State) Seal() int {
	s.sealed = true
	return len(s.entries)
}

// String renders the accumulator cardinality.
func (s *Envelope024State) String() string {
	return fmt.Sprintf("Envelope024(%d)", len(s.entries))
}
