[package]
name = "tracedecay-sessions"
version = "0.1.0"
edition.workspace = true
license = "MIT"
description = "TraceDecay session contracts and provider-neutral ports"
include = ["/src/**"]

[dependencies]
serde = { version = "1", features = ["derive"] }
