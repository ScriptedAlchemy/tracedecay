//! The authoritative configuration snapshot Context Scout pins against.
//!
//! Keep the agent-hosts port path as a compatibility shim over the canonical
//! configuration use-case contract.

pub use tracedecay_usecases::configuration::ConfigurationCurrentStateV1;
