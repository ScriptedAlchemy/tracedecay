[package]
name = "tracedecay-jsonrpc"
version = "0.1.0"
edition.workspace = true
license = "MIT"
description = "JSON-RPC 2.0 protocol types and line-transport contract for TraceDecay"
repository = "https://github.com/ScriptedAlchemy/tracedecay"

[lib]
doctest = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
