# tracedecay-capture

Root-free provider parsing, canonical projection, and stable source identity
kernels for TraceDecay capture. Runtime discovery, scheduling, sanitization,
admission, and persistence adapters remain in the root crate.
